{
  "compilerOptions": {
    "moduleResolution": "nodenext",

    // Aliases
    "baseUrl": ".",
    "paths": {
      "~/*": ["./src/*"]
    },

    // Linting
    "strict": true,

    /*
      I want only warnings for unused vars or params, not errors though.
      Looks like typescript by default gives warnings.

      Don't want eslint to change that.
    */
    "noUnusedLocals": true,
    "noUnusedParameters": true,

    "noFallthroughCasesInSwitch": true,

    /*
      Requires declaration of css files as modules, which vite already does vite-env.d.ts.
      Might have to set it up for other (later) projects.
      - https://www.typescriptlang.org/tsconfig/#noUncheckedSideEffectImports
    */
    "noUncheckedSideEffectImports": true,

    "noPropertyAccessFromIndexSignature": true
  }
}
